{% if resource_granted('tracker.issue_view') %}
    {% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

    <div class="responsive-cell">
        <div class="box-type1">
            <div class="title">
                <span class="widget-title">{{ "Issues reported by user"|trans }}</span>
            </div>

            <div class="row-fluid">
                {{ dataGrid.renderGrid('reported-issues-grid', {reporterId: entity.id}) }}
            </div>
        </div>
    </div>
{% endif %}